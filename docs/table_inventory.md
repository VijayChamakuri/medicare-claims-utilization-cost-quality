# Table inventory

> Generated by `medicare-claims reports` for profile `sample1`. Row counts are from that build.

| Table | Grain | Key | Rows |
|---|---|---|---:|
| `dim_date` | one row per calendar day | `date_key` | 1,461 |
| `dim_beneficiary` | one row per synthetic beneficiary | `beneficiary_id` | 116,352 |
| `dim_provider` | one row per provider ID and type (facility PRVDR_NUM or NPI) | `provider_type, provider_id` | 642,257 |
| `dim_diagnosis` | one row per normalized ICD-9-CM code seen on a claim | `diagnosis_code` | 13,408 |
| `dim_procedure` | one row per normalized HCPCS code seen on a claim | `procedure_code` | 8,991 |
| `dim_care_setting` | one row per care setting | `care_setting` | 3 |
| `fact_claim_header` | one row per claim (beneficiary + CLM_ID within a setting, segments merged) | `claim_key` | 5,587,855 |
| `fact_claim_line` | one row per carrier line, or per institutional HCPCS position | `claim_key, line_num` | 13,163,019 |
| `fact_claim_diagnosis` | one row per claim and distinct diagnosis code | `claim_key, diagnosis_code` | 12,950,443 |
| `fact_inpatient_stay` | one row per continuous inpatient stay | `stay_key` | 64,359 |
| `fact_beneficiary_year` | one row per beneficiary and year | `beneficiary_id, year` | 343,644 |
| `mart_member_month` | one row per beneficiary, year and month | `beneficiary_id, year, month` | 4,123,728 |
| `mart_utilization_monthly` | one row per month and care setting | `month_start, setting` | 108 |
| `mart_utilization_annual` | one row per year and care setting | `year, setting` | 9 |
| `mart_payment_monthly` | one row per month, setting and payment field | `month_start, setting` | 108 |
| `mart_payment_annual` | one row per year, setting and payment field | `year, setting` | 9 |
| `mart_beneficiary_year_summary` | one row per beneficiary and year | `beneficiary_id, year` | 343,644 |
| `mart_payment_concentration` | one row per year | `year` | 3 |
| `mart_demographic_summary` | one row per year, sex, race and age band | `year, sex, race, age_band` | 96 |
| `mart_member_risk` | one row per beneficiary and year | `beneficiary_id, year` | 343,644 |
| `mart_readmission_index` | one row per inpatient stay considered as an index | `stay_key` | 64,359 |
| `mart_quality_monitoring` | one row per discharge year and risk tier | `year, utilization_risk_tier` | 7 |
| `mart_condition_summary` | one row per condition, year and setting | `condition_type, year, setting, condition_id` | 2,286 |
| `mart_provider_performance` | one row per provider, provider type and year | `provider_type, provider_id, year` | 1,301,909 |
| `reconciliation_results` | one row per reconciliation check | `check_name` | 41 |
