# Field dictionary

> CMS synthetic claims - not real patient or provider performance.

Generated by `medicare-claims tableau`. One section per extract in `tableau/data/`.

## kpi_annual.csv

3 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `member_months` | number |  |
| `member_years` | number | Member months divided by 12. |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `payment_total` | number | Sum of payment_amount across settings. |
| `payment_per_beneficiary` | number |  |
| `payment_per_claim` | number |  |
| `claims_per_1000_member_years` | number |  |
| `admissions` | number | Continuous inpatient stays by admission date. |
| `admissions_per_1000_member_years` | number |  |
| `payment_per_admission` | number |  |
| `mean_length_of_stay_days` | number |  |
| `ed_proxy_visits` | number | Outpatient claims with HCPCS 99281 to 99285. Proxy, not a validated ED measure. |
| `ed_proxy_per_1000_member_years` | number |  |
| `outpatient_visits_per_1000_member_years` | number |  |
| `readmission_eligible_index` | number |  |
| `readmission_events` | number |  |
| `readmission_rate` | number | Readmitted eligible index stays / eligible index stays (30-day proxy). |
| `top_5pct_payment_share` | number |  |

## annual_by_setting.csv

9 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `setting` | text | Care setting: inpatient, outpatient or carrier. |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `member_years` | number | Member months divided by 12. |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `unique_beneficiaries` | number |  |
| `admissions` | number | Continuous inpatient stays by admission date. |
| `ed_proxy_visits` | number | Outpatient claims with HCPCS 99281 to 99285. Proxy, not a validated ED measure. |
| `outpatient_visits` | number |  |
| `carrier_service_lines` | number |  |
| `payment_source_field` | text |  |
| `payment_amount` | number | Medicare trust fund payment: CLM_PMT_AMT (inpatient, outpatient) or LINE_NCH_PMT_AMT_1..13 (carrier). Synthetic. Not a cost. |
| `payment_per_claim` | number |  |

## monthly_utilization.csv

108 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year_month` | text | Calendar month, YYYY-MM. |
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `setting` | text | Care setting: inpatient, outpatient or carrier. |
| `members` | number | Eligible members in the month. |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `unique_beneficiaries` | number |  |
| `admissions` | number | Continuous inpatient stays by admission date. |
| `ed_proxy_visits` | number | Outpatient claims with HCPCS 99281 to 99285. Proxy, not a validated ED measure. |
| `outpatient_visits` | number |  |
| `carrier_service_lines` | number |  |
| `claims_per_1000_members` | number |  |

## monthly_payments.csv

108 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year_month` | text | Calendar month, YYYY-MM. |
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `setting` | text | Care setting: inpatient, outpatient or carrier. |
| `payment_source_field` | text |  |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `payment_amount` | number | Medicare trust fund payment: CLM_PMT_AMT (inpatient, outpatient) or LINE_NCH_PMT_AMT_1..13 (carrier). Synthetic. Not a cost. |
| `negative_payment_claims` | number |  |
| `zero_payment_claims` | number |  |
| `payment_per_claim` | number |  |

## condition_summary.csv

2,286 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `condition_type` | text |  |
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `setting` | text | Care setting: inpatient, outpatient or carrier. |
| `condition_id` | text | AHRQ CCS 2015 category number, or 'unmapped'. |
| `condition_name` | text | AHRQ CCS category name, or a reason the code could not be grouped. |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `payment_amount` | number | Medicare trust fund payment: CLM_PMT_AMT (inpatient, outpatient) or LINE_NCH_PMT_AMT_1..13 (carrier). Synthetic. Not a cost. |
| `denominator_beneficiaries` | number |  |
| `prevalence` | number | Beneficiaries with the chronic condition flag / beneficiaries in the year. |

## provider_review_facility.csv

24,553 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `provider_type` | text |  |
| `provider_id` | text | Synthetic facility identifier (PRVDR_NUM). |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `payment_amount` | number | Medicare trust fund payment: CLM_PMT_AMT (inpatient, outpatient) or LINE_NCH_PMT_AMT_1..13 (carrier). Synthetic. Not a cost. |
| `payment_per_claim` | number |  |
| `peer_count` | number |  |
| `payment_per_claim_q1` | number |  |
| `payment_per_claim_q3` | number |  |
| `claims_q1` | number |  |
| `claims_q3` | number |  |
| `review_flag` | number | Peer-quartile review flag. Not fraud or quality evidence. |
| `reason_codes` | text | Why a provider-year was flagged. |

## readmission_review.csv

7 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `utilization_risk_tier` | text | Descriptive tier from prior-year information. Not CMS-HCC. |
| `eligible_index_stays` | number |  |
| `readmitted_stays` | number |  |
| `readmission_rate` | number | Readmitted eligible index stays / eligible index stays (30-day proxy). |
| `excluded_died_in_stay` | number |  |
| `excluded_insufficient_followup` | number |  |
| `stays_considered` | number |  |

## risk_tier_summary.csv

7 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `utilization_risk_tier` | text | Descriptive tier from prior-year information. Not CMS-HCC. |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `member_years` | number | Member months divided by 12. |
| `payment_amount` | number | Medicare trust fund payment: CLM_PMT_AMT (inpatient, outpatient) or LINE_NCH_PMT_AMT_1..13 (carrier). Synthetic. Not a cost. |
| `payment_per_beneficiary` | number |  |
| `admissions` | number | Continuous inpatient stays by admission date. |
| `admissions_per_1000_member_years` | number |  |

## payment_concentration.csv

3 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `top_count` | number |  |
| `top_payment` | number |  |
| `total_payment` | number |  |
| `top_share` | number | Share of payment held by the highest-paid 5 percent of beneficiaries. |

## demographic_summary.csv

96 rows. Aggregates only.

| Field | Type | Meaning |
|---|---|---|
| `year` | number | Calendar year (inpatient claims are dated by admission). |
| `sex` | text | Sex from the beneficiary summary (BENE_SEX_IDENT_CD). |
| `race` | text | Race from the beneficiary summary (BENE_RACE_CD): White, Black, Other, Hispanic. |
| `age_band` | text | Age at the end of the year from the synthetic birth date. |
| `beneficiaries` | number | Synthetic beneficiary rows in the year. |
| `member_months` | number |  |
| `claims` | number | Analytic claims (beneficiary + CLM_ID within a setting, segments merged). |
| `payment_amount` | number | Medicare trust fund payment: CLM_PMT_AMT (inpatient, outpatient) or LINE_NCH_PMT_AMT_1..13 (carrier). Synthetic. Not a cost. |
| `admissions` | number | Continuous inpatient stays by admission date. |
